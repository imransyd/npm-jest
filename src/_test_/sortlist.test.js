const makeHTML = require('../sortlist.js');

// här kommer testen

describe('sort the list', () => {
    
    test('test sort list', ()=>{
        
        list = [9,8,7,6,5,4,3];
        expect (actual).toBe(expected);
        
    });
    test('test sort list with words', ()=>{
        list = [ball, back, apple]
    })
    
});